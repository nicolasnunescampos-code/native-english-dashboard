import { createClient } from "@supabase/supabase-js";

const supabaseUrl = 'https://ibykhkbyzifddwnsfxxy.supabase.co';
// Need service role key to delete auth users or list them.
// But wait, do I have the service role key? I only have the anon key.
// Let me look at the `.env` file or something to get the service role key.
